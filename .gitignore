# Real config values (committed by individual projects, not this plugin repo)
docs/agents/*.md
!docs/agents/*.example.md

# OS / editor
.DS_Store
*.swp
*~

# Dependencies (if ever needed)
node_modules/
