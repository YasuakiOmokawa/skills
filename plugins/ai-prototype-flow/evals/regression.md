# ai-prototype-flow regression suite (empirical 検証の収束記録)

2026-07-11 の empirical-prompt-tuning (iter 1〜6、シナリオ A〜H) と 2026-07-12 の同 (iter 1〜11、
シナリオ I〜L) で使用したシナリオと要件チェックリスト。
skill (SKILL.md / references/ai_prototype_flow.md) を変更したら、変更箇所に対応するシナリオを
fresh executor (白紙の general-purpose subagent) で再実行し全 [critical] PASS を
確認する。収束時実績: A〜H は iter 5〜6、I〜L は iter 9〜11 + ホールドアウト L で
全シナリオ accuracy 100%・retries 0・過学習なし。

## 共通設定

- executor への制約: (1) 対象 2 ファイルを編集しない (2) 恒久成果物は対象案件ディレクトリのみ
  (一時ファイルは scratchpad 可) (3) `(人間: ...)` 停止点・ユーザー確認は提示扱いで進行
  (4) Skill ツールの /xxx 起動禁止。再入系はさらに「コード実装・PR 操作・/xxx・`! <command>`
  を含む作業項目は行単位で (検証環境につき省略)。正本が地の文で指示する取得・diff・記帳は実施」
- 検証 doc: 自分がアクセスできる任意の PRD 形式 gdocs を使い、doc ID とタブ ID
  (`t.<tabID>`) を差し替える。doc により実測値は変わる

## シナリオ A: 案件初期化 (タブ付き URL + 案件名指定)

依頼: `/ai-prototype-flow 案件初期化 <doc URL>?tab=t.<tabID>#heading=h.xxx 案件名 <eval名>`

1. [critical] ~/.claude/ai-prototype-flow-projects/<eval名>/ に prd_gdoc_snapshot_<日付>.md が作成され、中身が指定タブ (先頭見出し一致)
2. [critical] `data:image` 0 件かつ生エクスポート残存なし
3. plan_<eval名>.md 作成 + 冒頭 `PRD gdocs:` 行
4. `- <日付> 初期凍結スナップショット取得: <ファイル名>` 形式の進捗行
5. named version (Keep forever) の人間作業依頼が最終報告に含まれる
6. [critical] フェーズ 0〜3 を実行していない
7. `#heading=` を取得単位に影響させていない

## シナリオ B: 再入 PRD変更 (正常系)

fixture: plan (テンプレ準拠) + 凍結スナップショット (現行タブ export から任意の 1 節を
sed 削除した改変版を `prd_gdoc_snapshot_<過去日付>.md` として配置)。台帳・差分設計なし。
依頼: `/ai-prototype-flow 再入 PRD変更 — 取り込み先ブランチ無し (ドキュメント検証のみ)`

1. [critical] 開始時に再入ログ行数と dd_prd_delta*.md 数の照合を実施し報告 (0=0)
2. [critical] 凍結スナップショットと現時点エクスポートの diff を実際に取り D 分類の入力にする
3. dd_prd_delta.md (初回は日付なし) の命名で作成
4. [critical] progress-ledger `## 再入ログ` 節に規定形式で記帳 (台帳新規作成含む)
5. gdocs 本体を上書きしていない
6. 報告に照合・diff・記帳の結果が含まれる

## シナリオ C: 案件初期化 (タブ無し URL + 案件名未指定)

依頼: `/ai-prototype-flow 案件初期化 <doc URL (tab 無し)>`

1. [critical] ~/.claude/ai-prototype-flow-projects/<導出名>/ に画像除去済み全文スナップショット (数千行・複数タブ分)
2. 案件名が doc タイトル (接頭辞除去・機能名欄優先) から導出され宣言される
3. [critical] rclone copyid 経路、本文エクスポートは 1 回のみ (タイトル確認用の二度出しなし)
4. plan 作成 + `PRD gdocs:` 行 + 進捗行 + named version 依頼が揃う
5. フェーズ 0〜3 を実行していない

補足: 同一 doc ID の既存案件が 1 件あると重複検査が発火する (fallback: 新規作成 + 報告)。
2 件以上あると停止して確認する挙動になる (2026-07-11 の閾値追加後)。

## シナリオ D (ホールドアウト): 記帳漏れ疑いの照合発火

fixture: B と同じ + `dd_prd_delta_<過去日付>.md` (過去再入の痕跡) を置き、台帳は置かない
(= 差分設計 1 件 vs 再入ログ 0 行の食い違い状態)。依頼は B と同じ。

1. [critical] 照合で 1 vs 0 の食い違いを検出し「記帳漏れ疑い」として件数・対象ファイル付きで提示
2. 提示後の方針 (継続 / 停止) を明示 (規定: 検出・提示まで必須、遡及記帳せず、処理は継続可)
3. [critical] 今回の差分設計が既存と衝突しない日付付き命名 (dd_prd_delta_<今日>.md)
4. [critical] 再入ログの記帳 (台帳新規作成含む)
5. diff を実際に取り差分設計の入力にしている
6. gdocs 本体を上書きしていない

## シナリオ E: status 質問 (案件名付き)

2026-07-11 追加分 (E〜H): 無指定起動の案件順探索・status モード・着手記帳の検証。
empirical-prompt-tuning iter 1〜6 + ホールドアウト2回で収束 (最終3ラウンド
critical 全通過・accuracy 100%・過学習なし)。

E〜H の executor 制約は A〜D と一部異なる (「地の文」語の衝突を避けた行位置ベース):
「実行範囲: SKILL.md の Step 0・status モード・Step Z・実行規律の判定は実際に行う。
正本のフェーズ本体プロンプト列に入ったら、各作業項目 (/xxx・`! <command>`・地の文の
作業指示のいずれも) を成果物を作らず行単位で (検証環境につき省略) と記録して飛ばす。
非該当・条件不成立の判定だけは行ってよい。Step Z の完了行の要点句は
『検証実行につき本体省略』としてよい」

fixture: `~/.claude/ai-prototype-flow-projects/eval_phase_probe/` に plan_eval_phase_probe.md — 冒頭に
`実装対象リポジトリ: なし (検証用スタンドアロン — コードベース非依存で設計してよい)`、
`## 進捗` 節 (フェーズ0 完了・フェーズ1 完了の2行)、`## PoC 仮説 ledger` 節 (表3行)、
`## やらなかったこと` 節 (2項目) — と prd_eval_phase_probe.md (ローカルダミー PRD、
`PRD gdocs:` 行なし)。ledger 節が進捗節の後に続く構造が「末尾 vs 進捗欄」の
記帳先分裂 (iter 2 で検出) を再現するために必須。
依頼: `/ai-prototype-flow eval_phase_probe っていまどのフェーズだっけ？`

1. [critical] status モードに分岐し、フェーズ本体を実行しない (改良前ベースライン:
   実行依頼と解釈してフェーズ2 を走らせ、偽の完了行まで記帳した)
2. [critical] ファイル作成・編集 (記帳含む) 0 件
3. レポートに案件名・案件ディレクトリ / 完了済みフェーズ / 中断の有無 /
   次の一手 (フェーズ2) が揃う
4. 確認質問を発しない (提示のみで終了)

## シナリオ F: フェーズ語なしの継続 (案件名なし)

fixture: E と同じ。cwd = `~/.claude`、会話履歴は空。
依頼: `/ai-prototype-flow 続きやって`

1. [critical] 案件を (a) cwd 配下 (再帰) の `plan_*.md` / `*.progress-ledger.md` と
   (b) ~/.claude/ai-prototype-flow-projects/ の両方から集めて特定する。候補が複数なら候補一覧が現在地確認と同じ
   1 回の質問に相乗りされる (検証時の実環境では `~/.claude/plans/` 配下の別案件
   も (a) でヒットして 2 候補になる。iter 4 で executor が maxdepth 1 探索により
   これを見落とし critical 落ち → 「再帰・深さを絞らない」明文化で回復)
2. [critical] 現在地推定がフェーズ2 で、確認は 1 回のみ (パス等の追加質問なし)
3. [critical] フェーズ本体より前に `- <日付> フェーズ2 着手` を `## 進捗` 節内に
   追記し、Step Z の完了行を書いた後も着手行が残っている
4. 追記順が着手 → 完了で、いずれも `## 進捗` 節内 (ファイル物理末尾ではない)

## シナリオ G: 中断からの再開

fixture: E の plan に `- <日付> フェーズ2 着手` を追加 (対応する完了行なし)。
依頼: `/ai-prototype-flow eval_phase_probe の続きやって`

1. [critical] 着手行と完了行の不対応から中断を検出し、確認文言が
   「フェーズ2 の続きからでよいか」になる
2. 着手行を重複追記しない (既存行を起点に再利用)
3. 完了時は完了行のみ 1 行を `## 進捗` 節内に追記 (ファイル物理末尾ではない)

## シナリオ H (ホールドアウト): status 質問 (案件名なし・複数候補)

fixture: E と同じ。cwd = `~/.claude`、会話履歴は空。
依頼: `/ai-prototype-flow いまどこだっけ？`

1. [critical] status モードに分岐し、フェーズ実行・記帳なし (ファイル編集 0 件)
2. [critical] eval_phase_probe と既存の別案件の両候補を検出 (単一候補と誤認しない)
3. [critical] 候補選択の質問をせず、全候補の現在地を 1 レポートに並べて提示する
4. eval_phase_probe の次の一手 = フェーズ2、進捗欄を持たない別案件には
   記録形式外・推定である旨が明記される

## シナリオ I: フェーズ3 軽微スライス (fast path + progress-ledger 記帳)

2026-07-12 追加分 (I〜L): フェーズ3 実行規律・複数フェーズ連続実行・戻り先なし再入項目・
フェーズ0 成果物なし確認の検証。empirical-prompt-tuning iter 1〜11 + ホールドアウト L で収束。

fixture: `eval_ship_probe/` — plan (冒頭 `実装対象リポジトリ: なし (検証用スタンドアロン — 文書のみで
完結する案件)` + `## 進捗` にフェーズ0〜2.5 完了 4 行) / prd ローカルダミー / dd (タスク2件:
スライス1 = guideline.md への用語集節追加、スライス2 = 権限マトリクス節) /
`prd_<案件>.md.progress-ledger.md` (スライス2行とも未着手) / guideline.md (既存2節の骨組み)。
依頼: `/ai-prototype-flow フェーズ3 — 案件: <パス>、スライス1 をやって`
executor 制約は A〜D 系 + 「記録ラベルは skill 判定優先 (非該当 > 検証環境省略)」+
「検証環境の Skill 起動禁止はチェーン部分スキップ条項の実施不能と同列」。

1. [critical] フェーズ本体前に progress-ledger のスライス1 行を「実装中」へ更新し、プランファイルの
   進捗欄には着手行を追記しない
2. [critical] スライス1 を軽微と判定して fast path を選択し、判定根拠 (リスク領域 5 項目非該当) を宣言
3. DD 該当タスクを転記した簡易プラン (`plan_<案件名>_slice1.md`) を案件ディレクトリに作成
4. [critical] 完了時にスライス1 行の状態を更新し (新規行なし)、残スライス集計を提示して停止 —
   スライス2 へ自動で進まない
5. リポジトリ前提の作業項目を「非該当: リポジトリなし」と行単位で記録して飛ばし、文書作業
   (guideline.md への節追加) は実施する
6. 入力が揃っているため追加質問なしで進行する

## シナリオ J: 複数フェーズ明示指定「フェーズ0と1」

fixture: `eval_multi_probe/` — plan (repo なし、`## 進捗` 空) + prd ローカルダミー。
依頼: `/ai-prototype-flow フェーズ0と1 — 案件: <パス>`
executor 制約は E〜H 系 + 「Step Z 手順 1 の要約と次フェーズ名の提示は実施。省略可は進捗欄に
記帳する完了行の要点句のみ」。

1. [critical] フェーズ0 の本体前に着手行を `## 進捗` 節に追記し、フェーズ0 の Step Z 後に停止せず
   フェーズ1 へ続行する
2. [critical] フェーズ1 の本体前に着手行を追記し、Step Z で完了行を追記して終了 — フェーズ2 へ
   自動で進まない (次フェーズ名の提示のみ)
3. 進捗欄が 着手0 → 完了0 → 着手1 → 完了1 の 4 行、すべて `## 進捗` 節内
4. フェーズ0 成果物の有無確認 (Step 0 手順 5) を発しない (フェーズ0 を含む連続指定のため)
5. 現在地確認・案件選択の質問 0 回

## シナリオ K: 再入 正本取り違え (戻り先なし運用ルール項目)

fixture: `eval_canon_probe/` — plan (フェーズ2 完了行 + 設計メモ誤追記の記録行) / prd ローカルダミー /
`design_notes.md` (誤追記入りで新しい) / `design_notes.latest.md` (追記なしで古い)。台帳・差分設計なし。
依頼: `/ai-prototype-flow 再入 正本取り違え — 案件: <パス>。直近の設計メモ更新を誤って
design_notes.md へ追記してしまい、design_notes.latest.md と食い違っている`

1. [critical] 再入ルール節から「文書の正本管理」項目を選び、フェーズ本体を実行しない
2. [critical] 開始時に再入ログ行数と dd_prd_delta*.md 数の照合を実施し、結果 (0 = 0) を報告に含める
3. `design_notes.latest.md` が正本として最新内容を持ち、旧版冒頭に「旧版。正本は <実ファイル名>」の
   注記が入る。どちらの版の本文も失われない
4. 再入ログへの記帳を実施 (`→正本管理 適用` 読み替え、参照欄は適用先ファイル名の列挙。
   台帳新規作成含む)。案件プランファイルの進捗欄には書かない
5. gdocs・PRD・プランの既存記録を破壊しない

## シナリオ L (ホールドアウト): フェーズ1 単独指定・フェーズ0 成果物なし

fixture: `eval_start_probe/` — plan (repo なし、`## 進捗` 空) + prd ローカルダミー。技術調査メモなし。
依頼: `/ai-prototype-flow フェーズ1 — 案件: <パス>`。harness は確認への回答を
「最小工数の選択肢を選ぶ」と仮定して進行させる。

1. [critical] フェーズ0 成果物の不在を検出し、「フェーズ0 から始めるか、調査不要としてフェーズ1 に
   直行するか」の確認を 1 回提示する (無言でフェーズ0 を飛ばさない・無言でフェーズ0 を実行しない)
2. [critical] フェーズ1 の本体前に着手行を `## 進捗` 節に追記し、Step Z で完了行を追記して終了 —
   フェーズ2 へ自動で進まない
3. 確認は手順 5 の 1 回のみ (案件はパス明示のため追加質問なし)
4. 進捗欄の追記が `## 進捗` 節内にある

## シナリオ M: 案件初期化 タブ経路の権限 deny (委譲・対話不可)

依頼: 委譲 subagent として案件初期化のエクスポート工程を実行。タブ付き URL。
タブ取得コマンド (token 付き curl) は権限レイヤに deny 済み、再委譲・ユーザー対話は不可。

1. [critical] deny されたコマンドを verbatim でも言い換えでも再試行しない
2. [critical] rclone 全文経路へ縮退するか、それも不可なら未取得として明示停止・報告する (正本の縮退順 (1)-(3) に準拠)
3. スナップショット範囲が全文へ変わる旨と他タブ・子タブ混入の注意が親への報告に含まれる
4. 画像除去 → prd_gdoc_snapshot_<日付>.md 保存 → 生エクスポート削除 → `スナップショット範囲:` 行記録の後続手順が維持される

補足: ドライラン (実行計画書が成果物) で検証可能。実 gdocs 不要。2026-07-19 の実運用 deny 事案から追加。

## シナリオ N (ホールドアウト): フェーズ2 中断再開 (repo あり)

fixture: `eval_repo_probe/` — case/ に plan (冒頭 `実装対象リポジトリ: <実在するダミー React repo のパス>` +
`## 進捗` にフェーズ0/1 完了 + フェーズ2 着手のみ・対応完了行なし) / prd ローカルダミー。repo/ に
git init 済み最小 React プロジェクト (package.json + src/*.jsx、クリーンな作業ツリー)。
依頼: `/ai-prototype-flow 案件: <case パス> の続きやって`。harness は確認への回答を「続きから最小工数」と
仮定。executor 制約は E〜H 系 + 「実装対象リポジトリには一切変更を加えない (ブランチ作成・コミットも
省略記録に留める)」。品質パス各行が「該当」側に倒れる唯一のシナリオで、/xxx 行の停止点誤読 regression の
検出器 (2026-07-19 の react-doctor 行 /xxx 化検証から追加)。

1. [critical] 着手/完了行の不対応から中断を検出し、確認文言が「フェーズ2 の続きからでよいか」になる
2. [critical] 行単位記録の記法分類が字面に忠実で、repo 実在のためコード対象行を「非該当: リポジトリなし」と
   誤ラベルしない (品質パス各行 = /xxx 該当・検証環境につき省略)
3. [critical] 停止点 (ユーザーのコマンド実行結果待ち) 0・外部診断コマンドの Bash 実行 0
4. 完了行のみ 1 行を `## 進捗` 節内に追記 (ファイル物理末尾ではない)
5. repo のファイル・git 状態を変更しない

## 失敗パターン台帳 (I〜L 検証、2026-07-12)

- **前提付きフォーマットの縮退未定義**: 状態機械・記帳書式が持つ前提 (リポジトリ・戻り先フェーズ) が
  欠けるケースの読み替えが未規定で裁量補完が発生 (iter 1: スライス状態 5 値の縮退 / 再入ログの
  `→フェーズ<N>` 読み替え)。縮退規則の明文化で解消
- **ループ内終了処理の単位未定義**: フェーズ3 の Step Z 適用単位 (1 周 vs 全体) が読めない (iter 1)。
  「1 スライス = 適用単位」の明記で解消。ただし定義を置いた後、依存する下位手順の条件文
  (「フェーズ3 完了時」) の単位を書き直し忘れて iter 5 で再検出 — 単位を読み替える定義を置いたら
  依存箇所も同じ単位で書き直す
- **行内注記と一般規則の優先関係**: 「常に実行」注記が非該当判定を上書きするように読める (iter 2)、
  複数の非該当根拠の主根拠選択 (iter 5/8/9 と 3 回再発して初めて修正)、3 記法と非該当判定の順序
  (iter 10)。「注記は条件との関係のみ・非該当判定に先立たれる」「構造的判定を主根拠」で解消
- **要約ラベル・例示句の参照先未定義**: fast path の「品質パス」が指す行範囲 (iter 6)、
  「正本は .latest」の literal/プレースホルダ (iter 2) など、ラベルと実体の対応づけが推測任せ。
  参照先の明示で解消
- **同語異物の用語衝突**: 簡易プラン導入後、「プランファイルに着手行は書かない」の対象が主プランのみか
  曖昧 (iter 8)。「案件プランファイルにも簡易プランにも」への限定で解消。iter 11 で executor が
  この規則文を根拠に誤記帳を自己訂正しており、ガードレールとして機能
- **修正不要と判定した指摘** (いずれも挙動を変えないラベリング・記録粒度の揺れ):
  非該当ラベル文言と根本理由の乖離 / spike 等の抽象動詞の一般判定基準 (分割例が既に固定) /
  非該当句に依存する後続文書行の独立判定 (全 executor が一貫して独立適用) /
  fast path 各 skill のリポジトリ前提性は各 SKILL.md まで遡って確認する運用 (executor 側の実践)

## 失敗パターン台帳 (E〜H 検証、2026-07-11)

- **status 質問の実行誤爆**: 「〜だっけ？」を実行依頼と解釈しフェーズ実行+偽完了記帳
  (改良前ベースライン)。status モード新設 + 手順2 の振り分けで解消
- **探索範囲の解釈ぶれ**: 「配下」を maxdepth 1 と解釈し候補を見落とし (iter 4 critical)。
  「再帰 — 深さを絞らない」明文化で解消
- **記帳先の分裂**: 「末尾に追記」の字面適用で進捗行が `## 進捗` 節と物理末尾に分裂
  (iter 2)。「進捗欄 = `## 進捗` 節末尾」の用語定義で解消
- **条件付き詳細度の括弧注記**: 「(最終行は日付と要点も)」型の注記が両読みを生む
  (iter 4→5 で言い換えても再発)。条件性の削除 (「各行とも転記」) で解消 — 言い換えでなく
  自由度をなくすのが正解
- **軽量比較の基準欠如**: 既定候補の「最新」判定で候補の中身を深読み (iter 5 で 151K
  tokens)。「進捗欄の最終日付、無ければ mtime。中身は深読みしない」で解消 (iter 6 で 46% 減)
- **修正不要と判定した指摘**: 定義の前方参照 (DRY と衝突) / 「軽量」のさらなる線引き
  (過剰仕様化) / 規約導入前データへの适用は但し書きで対応済み

## 未解決の既知事項 (dispatcher 外、ユーザー判断待ち)

- 正本が参照する /grill-with-docs は `disable-model-invocation: true` のため Skill
  ツールから起動不可 (実行時は実行規律の類似名確認フローが毎回発火する)。
  frontmatter を外すか正本の参照名を変えるかは未決
- 過去案件の progress-ledger は6値 (計画中・対象外を含む)、正本フェーズ3 は5値。
  読み替え規則は未定義 (規約導入前データ)
- status モードに再入未記帳の検出義務を持たせるかは未定義 (現状は `再入` モード専用)
- 専用プランファイルを持たない案件で主文書候補 (PRD / DD 等) が複数あるときの進捗欄探索の
  優先順位は未定義 (2026-07-12 の H 再実行で検出。executor は全候補を確認してから
  「進捗欄なし」と判定したため実害なし)
- progress-ledger を新規作成するときのファイル見出し書式は未規定 (executor は既存実例に
  倣って `# progress-ledger: <案件名>` を採用しており揺れは未観測)
- 案件初期化節の残余の細部 (凍結範囲確認が括弧書き埋め込みで実行順が読み手依存 /
  生エクスポートの中間ファイル名未規定 / token 抽出式の完全形未固定)。2026-07-19 の
  empirical 検証 iter4 で executor から指摘されたが、いずれも自己解決が安定しており
  deny 縮退テーマの射程外のため resource cutoff で見送り
- 同 iter5 の残余指摘 (「事前に deny が判明」の判定基準が未固定 / `PRD gdocs:` 行の
  URL が所在記録と範囲指定の 2 役で縮退時に乖離 (executor は tab= 保持 +
  `スナップショット範囲:` 行を正本とする解釈で安定) / タブ無し URL の正規形例示なし /
  rclone 生エクスポートの特定手順未規定 / 正常時記帳の SSOT 分散)。いずれも
  自己解決が安定しており見送り

## 検証で確立した実測知見 (正本に反映済み)

- `tab=` エクスポートは指定タブ単体のみ (子タブ非含有)。経路・範囲の混在は偽 diff を生む
- token は表示・保存せずコマンド置換で curl に直接渡す (rclone.conf 直読み・dump 単独実行は不可)。
  統合コマンド自体も権限レイヤ (auto mode classifier) に credential 転用として deny されうる
  (2026-07-19 実測。subagent 委譲も文面によらず deny) — deny 時の縮退順は正本の案件初期化節に定義
- Docs API はrclone 共有 client では SERVICE_DISABLED (タブ木構造の機械取得は不可)
- rclone 共有 client_id は 2026 年中に廃止予定 (NOTICE 実測)。復旧手順は
  正本 (ai_prototype_flow.md 案件初期化節) を参照

## スリム化収束記録 (2026-07-17、v0.4.0)

empirical-prompt-tuning でスリム化を検証・収束。SKILL.md 216 行 / 19.5KB → 182 行 / 16.6KB。
案件初期化 (旧 91-115 行) と status モード (旧 117-132 行) の本体を verbatim で
`references/dispatcher_modes.md` (48 行) へ退避し、SKILL.md には「Step 0 手順 2 で判定したら
Read」の 1 hop ポインタ stub と Step 0 手順 2 内の分岐指示を残した。モード検出 (Step 0 手順 2)
は dispatcher の中核として SKILL.md に据え置き。狙いは common path (フェーズ実行) から
相互排他・低頻度な 2 モードの同居ノイズを外すこと。description は変更なし (全トリガーが本文で
カバー済み、Iter 0 でギャップなしを確認)。

fresh executor 2 ラウンド + ホールドアウトで全 [critical] ○・accuracy 100%・retries 0:

| ラウンド | シナリオ | [critical] | accuracy | tool_uses | dispatcher_modes.md 到達 |
|---|---|---|---|---|---|
| 1 | E (status 名前付き) | 全○ | 100% | 7 | ポインタ明確 |
| 1 | H (status 複数候補) | 全○ | 100% | 5 | ポインタ明確 |
| 1 | G (中断再開) | 全○ | 100% | 6 | 誤ルート無 (実行語で無指定) |
| 1 | A (案件初期化 名前付き) | 全○ | 100% | 7 | ポインタ明確 |
| 2 | C (案件初期化 名前なし+重複検査) | 全○ | 100% | 8 | ポインタ明確 |
| 2 | F (続きやって 複数候補) | 全○ | 100% | 7 | 誤ルート無 |
| 2 (hold-out) | I (フェーズ3 fast path) | 全○ | 100% | 12 | 非該当 (stub 素通り) |

- モードシナリオの tool_uses は 5〜8 で一定 — 退避による references-descent の skew なし
  (empirical-prompt-tuning の `tool_uses` skew 診断で問題なし)。I の 12 はフェーズ3 の実作業量
  (DD・ledger・guideline 読取り + 簡易プラン作成 + 2 ファイル編集) であって退避起因ではない
- スリム起因の新規不明点 0。executor が挙げた不明点は全て既存事項 (status モードでの正本 Read 要否・
  案件名 snake_case 粒度・fast path の 非該当 vs スキップ ラベル) か検証環境の gdocs 不可アーティファクトで、
  本改修とは独立。ホールドアウト I が構造の異なる common path で 100% のため過学習なし
- zero-rule-loss 確認: 退避 2 節は `dispatcher_modes.md` に verbatim 存在。SKILL.md の差分は
  Step 0 手順 2 の「(後述)」除去 + Read 指示追加のみ (「フェーズを実行しない」規則文は保持)

## 回帰再検証記録 (2026-07-18、無改修追認)

skill 無改修状態 (git HEAD、v0.4.0) に対する定期回帰。全 12 シナリオ A〜L を fresh executor
(白紙の general-purpose subagent、毎回新規起動・独立フィクスチャ) で再実行し、全 [critical] ○・
accuracy 100%・retries 0 で通過。SKILL.md / references への修正は無し (Iter 0 の description/本文
整合チェックもギャップなし — 2026-07-17 の判定を追認)。gdocs fetch 禁止環境のため、案件初期化
(A/C) のエクスポートと再入 diff (B/D) の現時点エクスポートはローカルの生エクスポート/凍結
スナップショット fixture でスタブし、dispatcher 判定・画像除去 sed・命名・diff・記帳・報告は実際に
実行させた (本 suite の「ローカルスナップショット等」設計に準拠)。前ラウンド (2026-07-17 スリム化収束)
が直前のクリアとして先行するため本日 1 ラウンドで確定。

| シナリオ | [critical] | accuracy | tool_uses | 新規不明点 |
|---|---|---|---|---|
| A (初期化 タブ付き+名前) | 全○ | 100% | 11 | 0 |
| B (再入 PRD変更 正常系) | 全○ | 100% | 13 | 1 テーマ (下記・修正不要) |
| C (初期化 タブ無し+名前導出) | 全○ | 100% | 8 | 0 |
| D (再入 記帳漏れ照合) | 全○ | 100% | 14 | 1 テーマ (B と同一・修正不要) |
| E (status 名前付き) | 全○ | 100% | 6 | 0 |
| F (続きやって 複数候補) | 全○ | 100% | 11 | 0 |
| G (中断再開) | 全○ | 100% | 8 | 0 (既知ラベル指摘のみ) |
| H (status 複数候補) | 全○ | 100% | 7 | 0 |
| I (フェーズ3 fast path) | 全○ | 100% | 13 | 0 (既知ラベル指摘のみ) |
| J (複数フェーズ 0と1) | 全○ | 100% | 11 | 0 |
| K (再入 正本取り違え) | 全○ | 100% | 13 | 0 |
| L (フェーズ1 単独・成果物なし) | 全○ | 100% | 9 | 0 |

- 新規観測 (修正不要と判定): 戻り先フェーズを持つ再入 (B/D) で、Step Z 手順2 の「通常のフェーズ完了行
  (進捗欄) に加えて再入ログ 両方必須」と、正本再入ログ共通規則の「記帳先を分散させない」が表層では
  相反して読める。B・D 両 executor とも独立に正しく解決した (通常のフェーズ着手/完了行は進捗欄、
  `再入(<イベント>)→フェーズN` 形式の再入記帳は再入ログ節のみ — 後者が「分散させない」の係り先)。当該
  進捗欄記帳は B/D いずれの checklist 項目でもなく全 [critical] に無影響で、「分散させない」の対象は
  再入記帳行のみで正常系と矛盾しないため regression としては修正不要。可読性負荷の軽減 (Step Z へ
  「再入記帳は再入ログ節のみ・通常フェーズ着手/完了行は進捗欄」の一文を足す等) は将来の capability
  パス候補として残す (regression 用途と混同しない)。
- G の /review-design 非該当ラベル・I の /review-plan-diff の 非該当 vs 検証環境省略は、いずれも既存
  「修正不要と判定した指摘」台帳 (E〜H・I〜L 検証) に収まるラベリング/
  記録粒度の揺れで挙動不変。全 executor が [critical] を正しく満たしている。
- 過学習なし: common path (フェーズ実行 E/F/G/J/L)・相互排他な低頻度モード (案件初期化 A/C・status E/H・
  再入 B/D/K)・フェーズ3 fast path (I) と構造の異なる 12 シナリオが全通過。dispatcher_modes.md への退避
  (2026-07-17) 後もモードシナリオの tool_uses は 6〜14 で安定し、references-descent の skew は未観測。

## 変更検証記録 (2026-07-18、v0.5.0)

実案件ふりかえり (KPT) から codify した 2 点を正本へ追記:
(1) フェーズ1 に PoC クローズ基準の宣言 — 仮説 ledger 全項目が grounded/killed で確定した時点で
PoC ブランチは役割終了・以後コミット禁止、後発の技術仮説は短命 spike を別に切る。
(2) 再入ルールに「PRD / Figma が凍結前 (フェーズ2 入り前) に動いた」項目 — コードに当てず
文書でのみ受け、コード適用は本実装で 1 回だけ。
根拠となった実測 (PoC 87 コミット中 feasibility 20% / 凍結後 Figma 反映 +141 行) は本文の理由句に記載。
変更箇所対応シナリオ B / D / K / L を fresh executor で再実行し、全 [critical] ○・accuracy 100%・retries 0:

| シナリオ | [critical] | 新設・追記箇所との関係 |
|---|---|---|
| B (再入 PRD変更 正常系) | 全○ | 凍結済み案件を既存「確定後」項目へ正しく分岐 (新設「凍結前」項目を誤選択しない。根拠 = 進捗のフェーズ2 完了行と凍結スナップショットの存在) |
| D (再入 記帳漏れ照合) | 全○ | 同上。照合・日付付き命名・台帳新規作成の既存動作に影響なし |
| K (再入 正本取り違え) | 全○ | 項目追加後も「文書の正本管理」を正しく選択、フェーズ本体不実行 |
| L (フェーズ1 単独・成果物なし) | 全○ | クローズ基準行を実行規律どおり句割りで処理 (宣言句 = 文書行為として実施記録・ブランチ句 = 非該当)。着手/完了記帳・Step Z 停止に影響なし |

- 新規不明点は 0 テーマ。L のクローズ基準行の句割り (行全体を非該当と読む別解釈の余地) は既存
  「リポジトリ前提句と文書作業句の混在 → 句ごと分割」規則の適用範囲内で挙動不変。B/D の再入時
  着手行・記帳先二重化テーマは 2026-07-18 回帰再検証記録の既知観測 (修正不要) と同一。

## 変更検証記録 (2026-07-19、v0.13.0 — empirical-prompt-tuning 収束)

empirical-prompt-tuning (iter 0〜ホールドアウト再実行) で SKILL.md / references / plugin.json / wrapper を改善し収束。
シナリオは M (deny 縮退・最新変更エッジ) / I (フェーズ3 fast path・中央値) / G (中断再開) を反復し、
収束判定後にホールドアウト K (正本取り違え) で過学習チェック → 命名 2 件を検出・修正し K 再実行で全○。
全ラウンドで全 [critical] ○・accuracy 100%・指示起因 retries 0 (途中の retry はすべて検証環境側の
fixture 競合・権限 deny によるもので、スキル文面起因ではない)。

適用した修正 (テーマ単位):

- Iter 0 (静的整合): SKILL.md の step_z 引用「手順 1-2」→「手順 1-3」(停止規則の引用落ち) /
  frontmatter・plugin.json の再入イベント列挙 3 種 → 本文の 5 種に整合 / wrapper usage を `<t.tabID>` 表記に統一
- Iter 2 (縮退の残余曖昧性): 縮退段(1) の導入手順はユーザーの事前設定 — エージェントが permissions.allow へ
  自己追加して認可を作らない・未許可 + 対話不能なら (1) 不成立を明文化 / URL は所在記録・範囲は
  `スナップショット範囲:` 行が正
- Iter 3 (記録の完成例): 縮退記帳の 1 行書式例 / 縮退で全文になった案件の URL 完成例 (tab= 残置)
- Iter 4 (適用対象の分離固定): 縮退記帳 = 遷移表記・テンプレート行 = 確定値
- Iter 5 (point-of-use 同居 — 独立 executor で 2〜3 回再発した 2 件を含む 5 件): Step Z 末尾要素が
  非該当のときの読み替えを SKILL.md 側に併記 / 文書入力 skill (/review-plan-diff・prose 系) は文書成果物が
  あれば適用 (非該当にしない) / 「実装対象リポジトリ: なし」明示記録は充足扱い・再質問しない /
  範囲 = 全文なら子タブ確認は不要 / dispatcher 手順 2 の名前導出エクスポートも縮退列に従う
  (全文縮退時は doc タイトルで命名)
- ホールドアウト後 (命名プレースホルダの実例固定): progress-ledger 既定名は拡張子込み連結
  (例: `prd_foo.md.progress-ledger.md`。plan 代用も `plan_<案件名>.md.progress-ledger.md` に統一) /
  旧版注記は「旧版。正本は <正本のフルファイル名>」(literal `.latest.md` を二重に付けない)

ラウンド実績 (fresh executor、毎回新規起動):

| ラウンド | シナリオ | [critical] | accuracy | steps | 新規の実行可能指摘 |
|---|---|---|---|---|---|
| iter 1 | M / I / G | 全○ | 100% | 6/14/6 | M 2 件 (→iter 2〜4 で修正)、I 3 件 (既知クラス・修正不要) |
| iter 2〜4 | M (iter 4 は I / G も) | 全○ | 100% | 5〜14 | 各 1〜5 件 → テーマ単位で修正 |
| iter 5 | M / I / G | 全○ | 100% | 7/16/10 | 0 (残 7 件は修正不要クラス) — クリア 1 |
| iter 6 | M / I / G | 全○ | 100% | 6/14/8 | 0 (I は不明点自体 0) — クリア 2 |
| hold-out | K | 全○ | 100% | 14 | 命名 2 件 (修正して再実行) |
| hold-out 再 | K | 全○ | 100% | 10 | 0 — 収束確定 |

失敗パターン台帳 (今回の追加分):

- **導入・設定手順の実行主体未指定**: ユーザー向け導入手順 (permissions.allow 追加等) がエージェント実行
  可能な指示に読める。権限の自己付与は対象外である旨を point-of-use に 1 句で明記して解消
- **命名プレースホルダの拡張子解釈**: `<prd ファイル名>` が拡張子込みか stem か読めず台帳名が分裂
  (ホールドアウト K で実測)。具体例 1 つの併記で解消 (K 再実行で確認)
- **point-of-use と解決規則の分離** (Step Z 末尾要素非該当の読み替え、文書入力 skill の適用境界 —
  独立 executor で 2〜3 回再発してから修正): 解決規則が別ファイルにのみあると再発する。
  トリガー宣言側に読み替えを併記する

修正不要と判定した指摘 (挙動不変・全 executor が独立に同一の正解へ自己解決):
記録規則の co-location 要望 (決定表化は capability 候補) / rclone 縮退 (2) が deny の巻き添えと誤読される
「余地」(4/4 executor 正解) / 「最後の適用項目」の句粒度 (句割り規則で一意) / mid-phase resume の
サブステップ台帳 (設計スコープ外・冪等再入で足りる) / /review-design の個別列挙 (文書入力 skill 規則で
一意化済み) / 条件不成立時の帰結句 / 案件初期化の「記帳しない」のスコープ (着手記帳のみ — 文脈で一意) /
統合後の旧版重複の扱い・参照欄の外延 (K の 2 run とも同一解)

未解決事項の更新: 旧「`PRD gdocs:` 行の URL が所在記録と範囲指定の 2 役で縮退時に乖離」は本 iter で
正本に明文化し解消。mid-phase checkpoint・記録規則の決定表化は capability パス候補として残す。

検証環境の教訓 (suite 運用): fixture を複数 executor・複数セッションで共有すると並走や中断再開で競合する。
ラウンドごとに fixture をリセットし、並走させる場合は executor ごとに fixture を分離すること。

## 変更検証記録 (2026-07-19、v0.14.0 — react-doctor 行の /xxx 化)

正本フェーズ2/フェーズ3 の品質パスにあった `! npx react-doctor@latest --verbose --diff` 行 (2 箇所) を
`/react-doctor` (skill 起動) へ変更し、execution_discipline.md の `! <command>` 記法説明から
react-doctor 例を除去 (記法定義自体は本 suite の executor 制約が参照するため残置。変更後の正本に
`! <command>` の現用行は 0)。empirical-prompt-tuning で検証し収束。

シナリオは I 変形 (フェーズ3 fast path — 品質パス範囲解決 + 行単位記法分類の [critical] 追加) と
G 変形 (フェーズ2 中断再開 — 同追加) を 2 ラウンド (fresh executor・ラウンドごと fixture 分離)、
収束後にホールドアウト N (repo あり中断再開 — 品質パスが該当側に倒れる逆分岐、本 suite に新規登録) で
過学習チェック。全 5 run で全 [critical] ○・accuracy 100%・指示起因リトライ 0:

| ラウンド | シナリオ | [critical] | accuracy | /react-doctor 行の扱い |
|---|---|---|---|---|
| 1 | I 変形 / G 変形 | 全○ | 100% | /xxx 分類・非該当: リポジトリなし。停止点/npx 実行 0 |
| 2 | I 変形 / G 変形 | 全○ | 100% | 同上。新規不明点 0 (I 側) / 既知クラスのみ (G 側) |
| hold-out | N (repo あり中断再開) | 全○ | 100% | /xxx 分類・該当 (Skill 起動対象、検証省略)。停止点/npx 実行 0 |

- 変更起因の新規不明点は全 5 run で 0。挙がった指摘はすべて既知クラス — /review-design の両属性境界
  (修正不要判定済み・全 executor が独立に同一の正解へ自己解決) / fast path「実装」語・/qa-ui 例外境界の
  参照先一般化 (要約ラベル類・挙動不変) / /grill-with-docs 未導入 (未解決の既知事項に記載済み) /
  mid-phase 再開粒度 (capability パス候補として既存) / Step Z「最後の適用項目」の適用可能性 vs
  成果物有無 (検証環境でのみ露出、通常運用では成果物が出るため衝突しない)
- Iter 0 (静的整合): description/本文ギャップなし。marketplace.json の ai-prototype-flow entry の
  description が v0.13.0 の再入イベント 5 種化に未追随だったため plugin.json と同期 (挙動無関係の記載整合)
- polish-before-commit 側の「外部診断ツール (react-doctor 等)」記述は skill 起動後の会話内指摘に
  そのまま適用可能なため変更不要と判定 (越境変更なし)
