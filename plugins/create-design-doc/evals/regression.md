# create-design-doc regression suite

skill 変更 PR では、白紙の fresh executor (general-purpose subagent) に SKILL.md パスとシナリオ + checklist を渡して再実行し、全 [critical] ○ を確認してから merge する。検証環境の約束: 他スキル起動 (/dry-ssot-text 等の品質パス、/map-user-stories 等) は「起動宣言 + 想定結果 1 行」で代替可。

## fixture (実行ごとに独立構築し、終了後に削除)

シナリオごとに別ディレクトリへコピーする (共有 fixture は並走で競合する)。

- `case/prototyping-projects/<案件名>/plan_<案件名>.md`: 冒頭メタ + 星取表 (実測値付き) + `## 申し送り (PoC → プロトタイプ)` + `## 申し送り (プロトタイプ → DD)` (所在 / 従った慣習 / 設計判断と根拠 + 採らなかった案 / PoC から変えた点 / スコープ外)
- `case/prototyping-projects/<案件名>/prototype/`: 慣習準拠の小コード一式 + git 履歴 2 コミット (PoC スクリプト → 慣習準拠で書き直し。申し送り欠落時の再構成材料になる)。`TooManyRows` 未定義・`actor` 未使用・`attributes` 素通しなど**意図的な欠落**を仕込む (「プロトタイプ側の欠落・矛盾も未確定に挙げる」の検証対象)
- `case/skills-config/create-design-doc/`: D1 / D3 はテンプレートを配置、D2 は**未配置のまま**にする (テンプレ欠如フォールバックが検証対象)。executor には「この実行では `~/.claude/skills-config` は `case/skills-config` を指す」と伝える (利用者の実ファイルに依存しない)

## シナリオ D1: 中央値 — 申し送りあり + テンプレートあり

依頼文: 「<案件名> の DD を作って」(案件ディレクトリを提示)

checklist:
1. [critical] `## 申し送り (プロトタイプ → DD)` 節と `prototype/` のコードを読んでから DD を作成し、実在するクラス / ファイル名が DD に反映されている
2. [critical] テンプレートの節構成 (見出しと番号) を保った DD になっている (独自構成に差し替えていない)
3. [critical] 設計判断が申し送りの根拠付きで転記され、採らなかった案が Did not adopt に残る
4. [critical] DD 完成後 (人間: DD レビュー → LGTM) で停止し、タスク分解・Jira 起票など後続工程へ勝手に進まない
5. 材料が無い節を捏造せず「該当なし」/「未確定」+ 理由で明示している
6. 手順 4 が指定する文章品質パスの適用 (宣言代替可)
7. DD が案件ディレクトリに `dd_<案件ディレクトリ名>.md` として保存され、パスが報告される

## シナリオ D2: エッジ — 申し送り節なし + テンプレート未配置 (2 つのフォールバックが同時)

依頼文: 「<案件名> のプロトタイプを設計書にして」(案件ディレクトリを提示)

checklist:
1. [critical] テンプレート未配置を検出し「テンプレートなしで作成」と宣言して進行 (エラー停止・差し戻しなし)
2. [critical] 申し送り節が無いことを検出し、コード / コミット履歴から設計判断・根拠・スコープ外を自分で整理してから DD を書く
3. [critical] DD に設計判断 + 根拠 + 採らなかった案 (Did not adopt 相当) が含まれる
4. [critical] DD 完成 + 人間のレビュー待ちで停止し、後続工程に進まない
5. コードから読めない事項 (既存経路の件数、UI 仕様など) を推測で断定せず未確定として明示。再構成した申し送り内容をプランファイルへ書き戻していない
6. 手順 4 が指定する文章品質パスの適用 (宣言代替可)
7. DD がファイルとして保存され、パスが報告される

## シナリオ D3: ホールドアウト — プロトタイプ PR が取得不能

fixture 差分: 申し送りの所在を PR 番号のみにし (`internal/expenses#482`)、`prototype/` を置かない。`gh` / ネットワークは使用不可。

依頼文: 「<案件名> の DD を起こして」

checklist:
1. [critical] PR が取得不能でも作業を止めず、申し送り節を材料に DD を作成している
2. [critical] テンプレートの節構成に沿っている
3. [critical] 設計判断 + 根拠 + Did not adopt が申し送りから転記されている
4. PR / コード未確認の事実を明示し、コードを見ないと確定しない具体 (request / response の詳細 schema など) を断定していない
5. 人間の DD レビュー待ちで停止している
6. DD がファイルとして保存され、パスが報告される

## 収束記録

- 2026-07-20 (v0.1.0): Iter 1・2 (D1) 連続クリア — 全 [critical] ○・accuracy 100%・新規不明点 0・retries 0。修正 0 件で収束
- 2026-07-25 (Opus 5 / Fable 5 tuning): 7 ラウンド / 15 executor 実行。全ラウンドで全 [critical] ○・accuracy 100% (D1 / D2 / ホールドアウト D3 とも)。修正 6 件 — 入力を実体で指定 / 保存先の既定値 / テンプレ骨組みと停止の命令化 / 品質パスの文書型境界 (3 スキル → 2 スキル) / 該当なし・未確定の使い分けとプロトタイプ欠落の未確定化 / Gotchas 1 行。品質パス chain の適用範囲が 4 ラウンド再発したため、読み物向け規範 (/cognitive-rhythm-writing) を chain から外して根治

### ラウンド別 (2026-07-25 分。accuracy はすべて 100%・成否はすべて ○)

| round | 修正テーマ | steps (D1 / D2) | duration (D1 / D2) | retries (D1 / D2) |
|---|---|---|---|---|
| 1 | baseline | 42 / 34 | 541s / 584s | 4 / 5 |
| 2 | 入出力の所在を実体で指定 | 34 / 26 | 531s / 546s | 2 / 1 |
| 3 | 規則の内在化 (骨組み・捏造禁止・停止の命令化) | 39 / 45 | 467s / 754s | 1 / 4 |
| 4 | 外部規範との衝突裁定 | 32 / 50 | 449s / 804s | 2 / 2 |
| 5 | 保護対象の追加・フォールバックの具体化 | 29 / 47 | 417s / 730s | 1 / 2 |
| 6 | chain 縮小 + テンプレ無し 6 節固定 (**hold-out D3 投入: 100% / steps 30 / 520s**) | 38 / 37 | 487s / 616s | 3 / 3 |
| 7 | ラベル選択基準 + プロトタイプ欠落の未確定化 | 34 / 35 | 539s / 612s | 1 / 5 |

accuracy は baseline から飽和していたため、主信号は **「指示の出どころ」** (checklist 各項目を skill 本文から導けたか、外部テンプレの注記や checklist で補ったか) に置いた。round 1 では節構成保持・捏造禁止・保存先・停止が本文の外に依存していたが、round 7 では本文 100% (残り 1 件は「報告にパスを含める」= skill の責務外)。

### 失敗パターン台帳

- **P1 品質パス chain の適用範囲・衝突裁定が未規定** — round 1・3・4・5 (局所修正 2 回では止まらず)。読み物向け規範を chain から外して根治。Fix rule: 汎用文章スキルを構造化文書に chain するなら適用範囲と衝突時の優先を書く。書けないなら chain から外す
- **P2 フォールバック経路の成果物形・入力読み替えが未定義** — round 1・2・4・5。テンプレ無し時を 6 節固定にして収束
- **P3 成果物の所在契約 (保存先・命名) の欠落** — round 1 → iter 1 で解消、以降 0
- **P4 終端が人間の行動の記述で agent 向け停止命令になっていない** — round 1・2・3 → iter 2 で解消、以降 0
- **P5 上流成果物の欠落・矛盾の落とし先が未定義** — round 3・4・5・6 → iter 6 で解消
- **P6 (意図的に未修正)** — chain 先の承認ゲートの通し方 / 宣言の出力先 / テンプレ記入ガイドの削除 / 入力コミットの記録。15 実行すべてで executor の解決が一致し失敗 0 件。特に承認ゲートは**評価ハーネスが対話を禁じたことによる人工物**で、ここに self-approve を書くと実運用の人間レビューを飛ばす方向に劣化する

### 確認ラウンド (2026-07-25、後任 conductor による収束後の再検証)

前任 conductor の成果物 (SKILL.md / 本ファイル) を引き継ぎ、その作業ディレクトリが消失していたため fixture を本ファイルの記述から再構築し、**D1 / D2 / D3 の 3 本を fresh executor で 1 回ずつ再実行**した。結果は 3 本とも全 [critical] ○ / accuracy 100%、修正 0 件。

| scenario | steps | duration | retries | 判定 |
|---|---|---|---|---|
| D1 (中央値) | 18 | 319s | 1 (推敲パスで番号ラベル除去・根拠の重複集約) | 全 7 項目 ○ |
| D2 (エッジ・二重フォールバック) | 18 | 362s | 1 (相互参照 ID の順序整合) | 全 7 項目 ○ |
| D3 (ホールドアウト) | 9 | 256s | 2 (根拠の再掲と外部制約の二重説明を参照に置換) | 全 6 項目 ○ |

機械検証した項目: テンプレートと DD の見出し一覧が D1 / D3 とも 17 / 17 完全一致 (節の追加・削除・改番なし)、プランファイルと `prototype/` が 3 本とも無変更 (= 再構成内容の書き戻しなし)、DD 本文にプラン固有の番号ラベル残存 0 件。

steps / duration は前任の round 7 (D1 34 / 539s、D2 35 / 612s) より大幅に少ないが、n=1 のため収束の証拠としては扱わない (下記の留保と同じ理由)。

**新規の actionable 不明点クラスは 0。** 3 実行の「迷った点」に再出したのは次の 3 件で、いずれも 3 executor が同じ解決に達し失敗 0 件のため P6 (意図的に未修正) に合流させる:

- **裏づけが弱い判断の置き場** — 上限行数 2,000 のように「実装に入っているが実測が無い」判断を、採用判断として書きつつ裏づけ不足を未確定にも載せる形で 3 本とも一致
- **材料皆無の節の埋め方** — 起票者は推測で人名を入れず未記入、テスト方針は設計判断から逆算、で 2 本とも一致 (手順 3 の「推測で埋めない」で足りている)
- **宣言の出力先** — 「テンプレートなしで作成」と品質パス適用の宣言を DD 冒頭に置くか末尾に置くかは本文未規定。既に P6 で記録済みの項目を再確認したもの

### tuning 進行状態 (2026-07-25)

収束済み (round 6・7 が連続クリア: 新規の actionable 不明点クラス 0、[critical] の落ち 15 実行で 0、hold-out D3 は 100% で低下 0 ポイント = 過学習なし)。上記の確認ラウンド (計 18 実行) でも修正 0 件。**残件なし** — 次に skill を変更する PR では上記 D1 / D2 を fresh executor で再実行し、全 [critical] ○ を確認すれば足りる。留保: steps / duration は n=1 のばらつきが厳格閾値 (±10% / ±15%) を超えるラウンドがあり、定量指標での収束は示していない (定性の飽和と resource cutoff で打ち切った)。version bump は未実施 (中央で処理)。
