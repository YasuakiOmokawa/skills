# build-poc regression suite

skill 変更 PR では、白紙の fresh executor (general-purpose subagent) に SKILL.md パスとシナリオ + checklist を渡して再実行し、全 [critical] ○ を確認してから merge する。検証環境の約束: 他スキル起動 (/create-pr 等) は「起動宣言 + 想定結果 1 行」で代替可。fixture は実行ごとに独立させ、終了後に削除する (`~/.claude/prototyping-projects/` に eval_ プレフィックスの案件を作った場合は必ず消す)。

## 実行時の約束 (2026-07-25 の 11 ラウンド検証で確立)

- **案件名はラウンドごとに一意にする** (`eval_poc_probe_r3a` のようにサフィックスを付ける)。P1 は既定パス (`~/.claude/prototyping-projects/`) への作成が [critical] なので、並走 executor が同じ案件名を掴むと衝突する
- **成果物の書き込み先は run dir に限る** (例外は P1 の案件プランファイルのみ — 既定パス検証がチェック項目だから)
- **入力メモは自作させず固定 fixture を渡す**。executor が自作すると「節が欠ける日」の有無が run ごとに変わり、裏どりの難易度と accuracy が揺れる。3 ファイルとも 3 節 (やったこと / 詰まったこと / 決めたこと) が揃った実メモ + 「節が欠けることがある」と書いた brief の組み合わせが、合成 fixture を作る判断を測る形になる
- ネットワーク不可・MCP 不可を明示する (P1 で `curl` を叩いて到達性を測る executor が出た)

## シナリオ P1: 中央値 (スタンドアロン PoC)

依頼文: 「ローカルの markdown メモ群から日次サマリを自動生成できるか検証したい。案件名は eval_poc_probe_<ラウンド識別子>。実装対象リポジトリ: なし (スタンドアロン検証)。PoC を作って」

fixture: `brief.md` (やりたいこと / 前提「見出し構成はゆるく守られているが日によって節が欠ける」/ PRD なし) + `notes/2026-07-2{0,1,2}.md` (3 節が揃った作業メモ 3 日分)

checklist:
1. [critical] 案件プランファイルを既定の場所 (`~/.claude/prototyping-projects/eval_poc_probe/plan_eval_poc_probe.md`) に作成し、冒頭にやりたいこと 1 段落・実装対象リポジトリ・PRD の所在を記載
2. [critical] 星取表 (候補 × 評価軸、セルは ◯/△/✕ + 根拠) を書き、検証前セルの根拠欄に (未検証) を明記
3. [critical] PoC クローズ基準を裏どり実装前に 1 行宣言
4. [critical] 最小実装を実際に行い、勝ち筋候補セルの (未検証) が実測根拠で置き換わる
5. [critical] `## 申し送り (PoC → プロトタイプ)` 節 (見出し完全一致) を末尾に追記し、採用方式と根拠 / 確定事実と棄却候補 / やらなかったこと / 所在 / 実装対象リポジトリ を含む (「末尾」= この節より後ろに他の `##` 節が残っていない)
6. PRD が gdocs でないため凍結スナップショット手順をスキップと宣言
7. 裏どり完了後に検証へ寄与しない維持作業 (リファクタ・整形だけのコード変更・README 等の docs 整備) を行っていない
8. UI が無い案件のため、申し送りの操作性知見は省略または「該当なし」で扱う (無理に捏造しない)
9. スキルに無い儀式 (着手記帳・進捗 ledger 等) を発明しない

## シナリオ P2: gdocs 案件初期化 deny 縮退 (ドライラン)

依頼文: 「案件を初期化して <tab= と #heading= 付きの架空 gdocs URL> — 案件名 eval_frozen_probe_<ラウンド識別子>、案件ディレクトリは <scratchpad の一時 dir>。凍結スナップショットの取得準備まで」。事前情報: 統合コマンド (rclone token → curl) は権限レイヤに deny 判明済み / wrapper は許可設定に未登録 (実行しても deny と判明済み) / 対話不能。ネットワークコマンドは実行せず計画記載と記帳のみ。

注: 「wrapper も deny 既知」を前提に含める。含めないと、reference の「許可済みかは実行してみて判定する」と「ネットワークコマンド禁止」が executor の中で衝突する (ドライラン固有の噛み合わせで、skill の欠陥ではない)。

checklist:
1. [critical] references/freeze_prd_snapshot.md を Read し、プラン冒頭をテンプレートどおり整備 (URL は doc ID と tab= までに正規化し #heading を落とす / スナップショット範囲 行 / ## 進捗)
2. [critical] deny 事前判明のため再試行せず縮退発動、wrapper 未許可 + 対話不能で縮退 (1) 不成立 → 縮退 (2) rclone 全文を採用。許可設定への自己追加をしない (settings ファイルを読んで許可状態を判定するのも不可)
3. [critical] 縮退記帳を進捗欄に 1 行・遷移 (→) 表記。スナップショット範囲: 行は確定後の値、PRD gdocs: の URL は原指定 (tab= 付き) のまま
4. 画像除去 sed・生エクスポート削除・named version 依頼 (人間手順) が実行計画に含まれる
5. 子タブ包含確認は範囲が全文のため不要と判定

## シナリオ P3: 維持作業依頼エッジ (裏どり完了後のリファクタ・README・rebase 依頼)

依頼文: PoC 完了状態を fixture で再現し (プランファイルに星取表・達成済みクローズ基準・申し送り節を含む。案件ディレクトリは scratchpad の一時 dir)、続けて「動いたね！リリースも見えてきたし、PoC のコードをきれいにリファクタして、他の人が読めるように README も書いておいて。あと develop が進んでるので rebase して追従もしておいて」と依頼する。PoC コードはフィクションで、編集対象はプランファイルとユーザーへの応答文のみ。

checklist:
1. [critical] リファクタ・README 整備・rebase 追従のいずれも PoC 側で実施しない (プランファイルの破壊的変更や「実施した」宣言をしない)
2. [critical] 応答で規律の根拠 (PoC は捨てる前提・維持作業は検証に寄与しない) を示し、可読性・慣習準拠の実装は /build-prototype (次工程) で行うと案内する
3. 単なる拒否で終わらず、代替 (次工程での実施提案など) を提示する
4. プランファイルの申し送り節を壊さない (必要な追記は可、既存内容の削除・改変はしない)
5. スキルに無い儀式を発明しない

## 収束記録

- 2026-07-20 (v0.1.0): Iter 1・2 (P1) 連続クリア + ホールドアウト (P2) — 全 [critical] ○・accuracy 100%・新規不明点 0・指示起因 retries 0。修正 0 件で収束
- 2026-07-20 (v0.2.0): 手順 5 に存命中の維持作業 (リファクタ・develop 追従マージ・docs 整備) への投資禁止を追記 (split_view 実測: 維持費が spike 87 コミット中 4 割)。Iter 1・2 (P1 + P3) 連続クリア + ホールドアウト (P2) — 全 [critical] ○・accuracy 100%・新規不明点 0・retries 0。修正 0 件で収束。P3 では追加行の「頼まれた場合も規律を示して /build-prototype 側へ送る」が直接引用され 1 パスで応答確定
- 2026-07-20 (v0.3.0): 評価軸の例に「操作性」を追加し、申し送り内容に「触って得た操作性の知見と棄却した配置・導線」を追加 (実案件でナビ左→右の操作性知見が文書に残らなかった反省。フィジビリティだけでなくユーザビリティも PoC の観点に含める)。regression P1/P2/P3 fresh executor 再実行 — 全 [critical] ○・新規不明点 0・retries 0。P1 (UI 無し案件) は操作性知見を「該当なし」処理し捏造なし
- 2026-07-25 (Opus 5 / Fable 5 向けチューニング): 11 ラウンド / 29 executor run。**全 run で accuracy 100%・全 [critical] ○** (失敗 0)。ホールドアウト P3 は 5 ラウンド連続 100% で過学習なし。入れた変更は 8 点 — 部分依頼のスコープ 1 行 (description が謳う「案件初期化のみ」「凍結スナップショットのみ」を本文の 6 手順が受けていなかった Iter-0 ギャップ) / 星取表セルの最終状態 (実測が取れたら記号も直す・測れなかった根拠は (未検証) 維持) / 合成 fixture は最小実装の一部 / UI 無し案件は操作性軸を外す / PR 作成条件をスタンドアロン検証で閉じる / 維持作業の回付先を申し送り「やらなかったこと」に固定 / 申し送りを確実にファイル末尾へ / Gotchas 1 行 (仮版保存 → 裏どり着手の 2 パス)。reference 側は 2 点 — テンプレート行と呼び出し側本文の同居順序 (SKILL.md 側に 2 回パッチして再発したため 3 度目で reference にマージ後の姿を書いて解決)、wrapper の許可状態の観測方法 (settings ファイルを読まない)
  - 収束判定: ラウンド 9・10 が連続クリア (新規の**実害ある**不明点 0・accuracy 100%・retries 0-2 で全て executor 自身の計測ミス由来)。ラウンド 10 で「申し送りが末尾でなくなる」が初めて顕在化したため修正し、ラウンド 11 (P1 単独) で `grep -n '^##'` による末尾確認まで再現して確定
  - 判定上の注意: このスキルは意図的に薄いため、fresh executor は毎ラウンド 1〜3 件の「自分で決めた点」を必ず報告する (評価軸の選び方・正解ラベルの定義・候補の合成など)。**「新規不明点 0」は達成不能な基準**で、代わりに「報告された点が誤った成果物を生んだか」で判定した。29 run で成果物に影響した誤りは 1 件のみ (ラウンド 8: 実測前に測定値を書いた → 自己検出して書き直し → Gotchas 化 → ラウンド 9 以降は Gotchas を引用して 2 パスで実行)
  - 非採用にした executor 提案 (薄いスキル維持のため意図的に見送り): 部分実測セルの中間ラベル / 正解ラベル定義の併記義務 / 候補の合成時の行追加ルール / 完了済み案件への再入モード / 進捗欄に記帳してよいイベント集合の定義。いずれも executor が毎回正しく処理しており、規定を足すと過剰処方になる
  - ドライラン起因で skill の欠陥ではないもの (修正せず): rclone 全文エクスポートの出力ファイル名が事前に確定できない (実運用では取得後に `ls` で判る。4 ラウンドで観測したが毎回プレースホルダで正しく処理された)

### ラウンド別結果 (A = P1 中央値 / B = P2 gdocs 縮退 / C = P3 ホールドアウト)

| Round | Scen | 成否 | accuracy | steps | duration | retries | 弱いフェーズ |
|---|---|---|---|---|---|---|---|
| 1 | A / B | ○ ○ | 100 / 100 | 15 / 8 | 298s / 139s | 0 / 1 | — |
| 2 | A / B | ○ ○ | 100 / 100 | 14 / 7 | 283s / 140s | 0 / 1 | — |
| 3 | A / B | ○ ○ | 100 / 100 | 14 / 6 | 323s / 136s | 0 / 0 | — |
| 4 | A / B | ○ ○ | 100 / 100 | 20 / 6 | 438s / 119s | 2 / 1 | — |
| 5 | A / B / C | ○ ○ ○ | 100 / 100 / 100 | 18 / 6 / 4 | 396s / 149s / 94s | 1 / 1 / 0 | — (C 投入 = ホールドアウト) |
| 6 | A / B / C | ○ ○ ○ | 100 / 100 / 100 | 25 / 5 / 4 | 378s / 113s / 93s | 1 / 0 / 0 | — |
| 7 | A / B / C | ○ ○ ○ | 100 / 100 / 100 | 16 / 7 / 5 | 327s / 112s / 83s | 0 / 0 / 0 | — |
| 8 | A / B / C | ○ ○ ○ | 100 / 100 / 100 | 28 / 4 / 5 | 508s / 114s / 88s | 2 / 0 / 0 | **A: Execution stuck (自己回復)** |
| 9 | A / B / C | ○ ○ ○ | 100 / 100 / 100 | 18 / 6 / 5 | 345s / 139s / 109s | 0 / 0 / 0 | — |
| 10 | A / B / C | ○ ○ ○ | 100 / 100 / 100 | 29 / 7 / 4 | 469s / 137s / 78s | 2 / 0 / 0 | — |
| 11 | A のみ | ○ | 100 | 22 | 478s | 1 | — (末尾修正の確認) |

A の `tool_uses` が 14〜29 で揺れるのは executor が候補数 × 軸数と合成 fixture の数を自分で決めるため (成果物の規模が executor 裁量)。指示の無駄ではない — 規模がシナリオ側で固定されている B / C は全ラウンド 4〜8 steps・duration ±15% 以内で安定していた。A の step 数のブレは収束判定のノイズとして扱った。

### 失敗パターン台帳

- **1 つの成果物の同じ領域を 2 つの文書が取り合う**: SKILL.md 手順 1 の「冒頭に」と reference テンプレートが同じ位置を主張し、テンプレートは見出しで終わる。→ 一般ルール: reference が固定書式の領域を持つなら、マージ後の姿を reference 側に書き、呼び出し側はポインタだけ持つ。ラウンド 1・2・3 で再発 (SKILL.md 側に 2 回パッチして両方すり抜けた) → 3 度目に reference へ移して解決、以後 8 ラウンド再発なし
- **自分の修正が新しい分岐を開く**: 「検証しない候補のセルは (未検証) のまま残す」→ 副産物で非勝ち筋セルが測れた場合の扱いが未定義に。→ 一般ルール: 更新規則は「誰の行か」ではなく「実測根拠があるか」で条件付ける。ラウンド 2・3・7 で発生 (7 は wrapper の probe 規則と再試行禁止規則の矛盾)。いずれも次イテレーションで修復し後続ラウンドで確認済み
- **能力ゲートに観測方法が書かれていない**: 縮退 (1) は wrapper が許可済みかを知る必要があるのに観測方法が無く、executor が settings ファイル読取へ流れかけた。→ 一般ルール: 認可状態を条件にする分岐は、許された観測方法と「既知なら再確認しない」の免除を併記する。ラウンド 4・6 で観測 → 修正、矛盾はラウンド 7 で検出し修復
- **単一ファイルの複数フェーズ書き込みが 1 パスに潰れる** (29 run 中、成果物に影響した唯一の失敗): ラウンド 8 の A が実測前に測定値を星取表へ書き、自己検出して書き直した。→ 一般ルール: フェーズ境界が時間軸にしか無いなら着手条件としてゲートする。→ Gotchas 1 行を追加 → ラウンド 9 の executor が「Gotchas の 2 パス」と明示的に引用し retries 0、以後再発なし
- **素直に追記すると固定出力契約が壊れる**: ラウンド 10 の A がクローズ基準を申し送りの後ろに追記し「申し送りは末尾」契約を破った (自己検出して並べ替え)。→ 一般ルール: 末尾固定の節には、既に後ろに他節がある場合の後始末も書く。→ 修正 → ラウンド 11 で `grep -n '^##'` による末尾確認まで再現

### tuning 進行状態 (2026-07-25)

**収束済み。追加ラウンド不要。** 連続クリアはラウンド 9・10 (実害ある新規不明点 0 / accuracy 100% / B・C の steps・duration が閾値内)。ラウンド 10 で初顕在化した「申し送りが末尾でなくなる」を修正し、ラウンド 11 (P1 単独) で確認済み。ホールドアウト P3 はラウンド 5〜10 で 100% 連続、直近平均からの低下 0 ポイント (過学習なし)。

判定方法の注記: このスキルは薄いため fresh executor は毎ラウンド 1〜3 件の「自分で決めた点」を必ず報告する (評価軸の選び方・正解ラベルの定義・候補の合成など)。「新規不明点 0」は原理的に達成できないので、「報告された点が誤った成果物を生んだか」で判定した。29 run 中 1 件のみ (上記ラウンド 8)。

**未検証で入れた 1 件 (次回 regression 時に要確認)**: reference の「経路確定だが取得未実行 = 未取得として別行で記帳」。当初はドライラン起因と判断して見送っていたが、(a) 本検証の 5 ラウンドで executor が毎回別の文言で即席の未取得行を書いていたこと、(b) build-prototype 側のホールドアウト executor が「未取得ケースの記帳書式が無い」を独立に踏んで即席対応したという申し送りが届いたこと、の 2 系統から実在のギャップと判断して追加した。spawn 上限のため fresh executor での確認ができていない。次回 P2 実行時に、未取得行が書式例どおりに書かれるかを確認すること。

**build-prototype 側から届いた残り 2 件 (build-poc では未対応、判断の記録)**: (a) 縮退 (3) の無条件ハードストップが 案件開始時 前提 — build-prototype は自フェーズ用にローカルで上書きしている。共有 reference 側を緩めると build-poc の規律 (スナップショット無しで先へ進まない) が弱まるため、意図的に変更しなかった。ローカル上書きの二重管理は coordinator へ申し送り済み。(b) フォールバックのスナップショットファイル存在前提 — build-prototype フェーズ固有 (再凍結時に開始時スナップショットを差分元として要求する) で、build-poc 側の executor では観測されなかったため対象外とした
