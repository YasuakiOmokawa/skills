---
type: llm
---
The reply explains how the prefix search should be integrated: which layer holds the index (service vs handlers vs store), how the query normalization and the empty-result contract map onto the existing code, and what tests would cover it. It does not claim to have changed any file. Pass if the placement is explained and no change is claimed.
